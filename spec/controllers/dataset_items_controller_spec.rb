require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.

describe DatasetItemsController do

  # This should return the minimal set of attributes required to create a valid
  # DatasetItem. As you add validations to DatasetItem, be sure to
  # update the return value of this method accordingly.
  def valid_attributes
    {}
  end
  
  # This should return the minimal set of values that should be in the session
  # in order to pass any filters (e.g. authentication) defined in
  # DatasetItemsController. Be sure to keep this updated too.
  def valid_session
    {}
  end

  describe "GET index" do
    it "assigns all dataset_items as @dataset_items" do
      dataset_item = DatasetItem.create! valid_attributes
      get :index, {}, valid_session
      assigns(:dataset_items).should eq([dataset_item])
    end
  end

  describe "GET show" do
    it "assigns the requested dataset_item as @dataset_item" do
      dataset_item = DatasetItem.create! valid_attributes
      get :show, {:id => dataset_item.to_param}, valid_session
      assigns(:dataset_item).should eq(dataset_item)
    end
  end

  describe "GET new" do
    it "assigns a new dataset_item as @dataset_item" do
      get :new, {}, valid_session
      assigns(:dataset_item).should be_a_new(DatasetItem)
    end
  end

  describe "GET edit" do
    it "assigns the requested dataset_item as @dataset_item" do
      dataset_item = DatasetItem.create! valid_attributes
      get :edit, {:id => dataset_item.to_param}, valid_session
      assigns(:dataset_item).should eq(dataset_item)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "creates a new DatasetItem" do
        expect {
          post :create, {:dataset_item => valid_attributes}, valid_session
        }.to change(DatasetItem, :count).by(1)
      end

      it "assigns a newly created dataset_item as @dataset_item" do
        post :create, {:dataset_item => valid_attributes}, valid_session
        assigns(:dataset_item).should be_a(DatasetItem)
        assigns(:dataset_item).should be_persisted
      end

      it "redirects to the created dataset_item" do
        post :create, {:dataset_item => valid_attributes}, valid_session
        response.should redirect_to(DatasetItem.last)
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved dataset_item as @dataset_item" do
        # Trigger the behavior that occurs when invalid params are submitted
        DatasetItem.any_instance.stub(:save).and_return(false)
        post :create, {:dataset_item => {}}, valid_session
        assigns(:dataset_item).should be_a_new(DatasetItem)
      end

      it "re-renders the 'new' template" do
        # Trigger the behavior that occurs when invalid params are submitted
        DatasetItem.any_instance.stub(:save).and_return(false)
        post :create, {:dataset_item => {}}, valid_session
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested dataset_item" do
        dataset_item = DatasetItem.create! valid_attributes
        # Assuming there are no other dataset_items in the database, this
        # specifies that the DatasetItem created on the previous line
        # receives the :update_attributes message with whatever params are
        # submitted in the request.
        DatasetItem.any_instance.should_receive(:update_attributes).with({'these' => 'params'})
        put :update, {:id => dataset_item.to_param, :dataset_item => {'these' => 'params'}}, valid_session
      end

      it "assigns the requested dataset_item as @dataset_item" do
        dataset_item = DatasetItem.create! valid_attributes
        put :update, {:id => dataset_item.to_param, :dataset_item => valid_attributes}, valid_session
        assigns(:dataset_item).should eq(dataset_item)
      end

      it "redirects to the dataset_item" do
        dataset_item = DatasetItem.create! valid_attributes
        put :update, {:id => dataset_item.to_param, :dataset_item => valid_attributes}, valid_session
        response.should redirect_to(dataset_item)
      end
    end

    describe "with invalid params" do
      it "assigns the dataset_item as @dataset_item" do
        dataset_item = DatasetItem.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        DatasetItem.any_instance.stub(:save).and_return(false)
        put :update, {:id => dataset_item.to_param, :dataset_item => {}}, valid_session
        assigns(:dataset_item).should eq(dataset_item)
      end

      it "re-renders the 'edit' template" do
        dataset_item = DatasetItem.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        DatasetItem.any_instance.stub(:save).and_return(false)
        put :update, {:id => dataset_item.to_param, :dataset_item => {}}, valid_session
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested dataset_item" do
      dataset_item = DatasetItem.create! valid_attributes
      expect {
        delete :destroy, {:id => dataset_item.to_param}, valid_session
      }.to change(DatasetItem, :count).by(-1)
    end

    it "redirects to the dataset_items list" do
      dataset_item = DatasetItem.create! valid_attributes
      delete :destroy, {:id => dataset_item.to_param}, valid_session
      response.should redirect_to(dataset_items_url)
    end
  end

end
