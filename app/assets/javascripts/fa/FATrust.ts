export default class FATrust {
    public annotatedCount: number;
    public totalCount: number;

    constructor(annotatedCount: number, totalCount: number) {
        this.annotatedCount = annotatedCount;
        this.totalCount = totalCount;
    }
}