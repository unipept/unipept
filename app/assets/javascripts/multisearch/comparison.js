/**
 * Constructs a PeptideComparison object that handles all JavaScript of the
 * metaproteomics analysis results page
 *
 * @return <PeptideComparison> that The constructed PeptideComparison object
 */
var constructPeptideComparison = function constructPeptideComparison(args) {
    /*************** Private variables ***************/

    var roots = args.roots;


    /*************** Private methods ***************/

    /**
     * Initializes Peptide comparison
     */
    function init() {
        // initialize visualization

        // set up save images

        // set up download datasets

        // set up fullscreen

    };
};
