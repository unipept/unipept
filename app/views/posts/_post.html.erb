<h3>
  <%= post.title %>
  <span class="posted-on"> posted on <%= post.date %></span>
</h3>

<p>
  <%= post.content.html_safe %>
</p>

<% if session[:admin] %>
	<%= link_to 'Edit', edit_post_path(post) %> |
	<%= link_to 'Delete', post, :confirm => 'Are you sure?', :method => :delete %>
<% end %>