<div class='form-group' id="<%= id_prefix + "qs-input-group" %>">
  <label class="control-label" for="qs">Peptide list</label>
  <%= text_area_tag(id_prefix + 'qs', nil, :rows => 7, :spellcheck => "false", :class => "form-control input-item", autofocus: "true") %>
  <span class="help-block hidden" id="<%= id_prefix + "help-block-qs" %>">At least one peptide is required</span>
</div>
<div id="multisearch-warning" class='alert alert-warning' role='alert'><strong>Oh snap!</strong> You're trying to analyse <strong class="multisearch-warning-amount">a lot of</strong> peptides. Searching for this many peptides might take a while and the resulting visualisations will probably be suboptimal. Our <a href="<%= api_apidocs_path %>">webservices</a> are a great alternative.</div>
<div class="form-group" id="<%= id_prefix + "name-input-group" %>">
  <label class="control-label" for="search_name">Name this dataset</label>
  <%= text_field_tag(id_prefix + 'search_name', nil, :placeholder => 'e.g. Sample B5', :class => "form-control js-has-focus-tooltip input-item", title: "This name will be shown on the results page. Handy if you have many open tabs.")%>
  <span class="help-block hidden" id="<%= id_prefix + "help-block-name" %>">Name is required when the dataset is set to be saved</span>
</div>
<div class="checkbox">
  <%= label_tag(:save_dataset, check_box_tag(id_prefix + 'save_dataset', 1, true, class: "input-item") + " Save dataset", title: "Store dataset in browser's local storage", class: 'js-has-hover-tooltip') %>
</div>
