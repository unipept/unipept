<%= form_tag(:sequence_multi_search, :method => "post") do %>
	<%= label_tag(:p, "Search for") %>
	<%= text_area(:p, "", :cols => 63, :rows => 5) %>
	<br/>
	<%= check_box_tag(:drafts, "", :selected => true)%>
	<%= label_tag(:drafts, "Include draft genomes")%>
  <%= submit_tag("Search") %>
<% end %>

<%= form_tag(:sequence_multi_search2, :method => "post") do %>
	<%= label_tag(:q, "Search for") %>
	<%= text_area(:q, "", :cols => 63, :rows => 5) %>
	<br/>
  <%= submit_tag("Search") %>
<% end %>